<?php

die(); //Don't actually run the following code.

$stylesheetpath = STYLESHEETPATH;
echo TEMPLATEPATH;

// Don't throw an error on the following
/* Let's not use STYLESHEETPATH */
$my_templatepath = MYTEMPLATEPATH;